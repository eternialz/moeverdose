<%= form_for @tag, as: "tag", :url => { :action => "update" }, html: { class: "zone" } do |f| %>
    <h2>Aliases of tag "<%= @tag.names[0] %>"</h2>

    <textarea name="names"><%= @names %></textarea>

    <button form="edit_tag" class="btn btn-green" type="submit">
        Save <%= fa_icon "save" %>
    </button>
<% end %>
